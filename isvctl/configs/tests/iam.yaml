# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary

# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

# IAM User Lifecycle Validation - Canonical Configuration
#
# THE canonical IAM test suite. Provider configs import this file and
# override commands with platform-specific stubs.
#
# Architecture:
#   - Scripts: Platform-specific (providers implement these) - do all IAM work
#   - Validations: Platform-agnostic (defined here) - just check JSON output
#
# Contract:
#   Each script MUST print a JSON object to stdout. The validations
#   only inspect that JSON. As long as your scripts output the right
#   fields, the validations pass regardless of which IAM system you use.
#
# Steps:
#   1. create_user  (setup)    - Create a user account, output JSON
#   2. test_credentials (test) - Verify the credentials work, output JSON
#   3. teardown     (teardown) - Delete the user, output JSON
#
# Quick start (new provider):
#   1. Create your provider folder: mkdir -p isvctl/configs/providers/my-isv/
#   2. Create providers/my-isv/iam.yaml with: import: ../../tests/iam.yaml
#   3. Override commands with your platform stubs
#   4. Run: uv run isvctl test run -f isvctl/configs/providers/my-isv/iam.yaml
#
# See also:
#   - AWS reference implementation: aws/iam.yaml + stubs/aws/iam/
#   - JSON schemas: isvctl/src/isvctl/config/output_schemas.py

version: "1.0"

commands:
  iam:
    phases: ["setup", "test", "teardown"]
    steps:
      # ─── Step 1: Create User ───────────────────────────────────────
      # YOUR SCRIPT must output JSON with at minimum:
      #   {
      #     "success": true,
      #     "platform": "iam",
      #     "username": "<created-username>",
      #     "user_id": "<unique-id>",
      #     "access_key_id": "<credential-id>",       # if applicable
      #     "secret_access_key": "<credential-secret>" # if applicable
      #   }
      - name: create_user
        phase: setup
        command: "python3 ../stubs/iam/create_user.py"
        args:
          - "--username"
          - "isv-test-user"
        timeout: 60

      # ─── Step 2: Test Credentials ──────────────────────────────────
      # YOUR SCRIPT must output JSON with at minimum:
      #   {
      #     "success": true,
      #     "platform": "iam",
      #     "account_id": "<account-or-tenant-id>",
      #     "tests": {
      #       "identity": {"passed": true},
      #       "access":   {"passed": true}
      #     }
      #   }
      #
      # The args below use Jinja2 templating to forward values from
      # the create_user step output. Adjust arg names to match your script.
      - name: test_credentials
        phase: test
        command: "python3 ../stubs/iam/test_credentials.py"
        args:
          - "--username"
          - "{{steps.create_user.username}}"
          - "--credential-id"
          - "{{steps.create_user.access_key_id}}"
          - "--credential-secret"
          - "{{steps.create_user.secret_access_key}}"
        sensitive_args: ["--credential-secret"]
        timeout: 60

      # ─── Step 3: Teardown (Delete User) ────────────────────────────
      # YOUR SCRIPT must output JSON with at minimum:
      #   {
      #     "success": true,
      #     "platform": "iam",
      #     "resources_deleted": ["user:<username>"],
      #     "message": "User deleted successfully"
      #   }
      - name: teardown
        phase: teardown
        command: "python3 ../stubs/iam/delete_user.py"
        args:
          - "--username"
          - "{{steps.create_user.username}}"
          - "{{teardown_flag}}"
        timeout: 120

tests:
  cluster_name: "iam-validation"
  description: "IAM user lifecycle validation"
  platform: iam

  settings:
    # Override these for your environment
    region: "us-west-2"
    teardown_flag: "{{(env.IAM_SKIP_TEARDOWN == 'true') | ternary('--skip-destroy', '')}}"

  # ─── Validations ─────────────────────────────────────────────────
  # These are PROVIDER-AGNOSTIC. They only check JSON field names/values.
  # You should NOT need to change these unless you add extra checks.
  validations:
    setup_checks:
      step: create_user
      checks:
        FieldExistsCheck:
          fields: ["username", "access_key_id"]
        StepSuccessCheck: {}

    credentials:
      step: test_credentials
      checks:
        FieldExistsCheck:
          field: "account_id"
        StepSuccessCheck: {}

    teardown_checks:
      step: teardown
      checks:
        StepSuccessCheck: {}

  exclude:
    markers: []
